# Issue #20: 開発環境で自動生成されるファイルの除外設定

## 📋 概要
開発環境で自動生成されるファイルやビルド成果物がリポジトリに含まれており、以下の問題が発生しています：
- リポジトリサイズの不要な増大（`go/server`は約30MB）
- 環境依存のファイルによるマージコンフリクトのリスク
- コードレビュー時のノイズ増加

本issueでは、これらのファイルを適切に除外設定し、クリーンなリポジトリ状態を維持します。

## 🎯 対象ファイル

### Go関連
- `go/server` - コンパイル済みサーバーバイナリ（約30MB）
- `go/tmp/` - Air（ホットリロードツール）の一時ファイル
- `.air.log` - Airのログ出力
- `.air.toml.log` - Air設定のログ

### その他
- IDE固有の設定ファイル（必要に応じて）
- ビルド中間生成物

## ✅ 実装手順

### 1. `.gitignore`の更新
以下のエントリを追加：
```gitignore
# Go build artifacts
go/server
go/tmp/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Air (Go hot reload)
.air.log
.air.toml.log

# Test binary
*.test

# Output of the go coverage tool
*.out
```

### 2. 既存の追跡ファイルの削除
```bash
# バイナリファイルの削除
git rm --cached go/server

# 一時ディレクトリの削除（存在する場合）
git rm -r --cached go/tmp/ 2>/dev/null || true

# ログファイルの削除（存在する場合）
git rm --cached .air.log 2>/dev/null || true
git rm --cached .air.toml.log 2>/dev/null || true
```

### 3. リポジトリの監査
```bash
# 大きなファイルを検出
find . -type f -size +1M -not -path "./.git/*" -exec ls -lh {} \;

# 自動生成される可能性のあるファイルを検出
find . -type f \( -name "*.log" -o -name "*.tmp" -o -executable -type f \) -not -path "./.git/*"
```

### 4. 変更のコミット
```bash
git add .gitignore
git commit -m "chore: exclude auto-generated files from version control

- Add Go build artifacts to .gitignore
- Add Air hot-reload temporary files to .gitignore
- Remove committed go/server binary (30MB)
- Improve repository cleanliness

Fixes #20"
```

## 🧪 確認方法
1. `git status`で除外対象ファイルが表示されないことを確認
2. ビルドを実行して`go/server`が生成されることを確認
3. `git status`で`go/server`が表示されないことを確認
4. リポジトリサイズの削減を確認：`du -sh .git/`

## 📝 チェックリスト
- [ ] `.gitignore`にGoビルド成果物のエントリを追加
- [ ] `.gitignore`にAir関連ファイルのエントリを追加
- [ ] `go/server`をgitの追跡から削除
- [ ] 一時ディレクトリ`go/tmp/`の除外設定（存在する場合）
- [ ] ログファイルの除外設定
- [ ] リポジトリ内の大きなファイルの監査完了
- [ ] 変更のコミット＆プッシュ
- [ ] ビルドテストの実行確認

## 🔗 関連情報
- [Gitignore for Go projects](https://github.com/github/gitignore/blob/main/Go.gitignore)
- [Air - Live reload for Go apps](https://github.com/cosmtrek/air)

## 💡 今後の改善案
- CI/CDパイプラインでビルド成果物のサイズチェックを追加
- pre-commitフックで大きなファイルのコミットを防止
- `.gitattributes`でバイナリファイルの扱いを明示
